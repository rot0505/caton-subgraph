{
  "name": "caton",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy caton-subgraph ./subgraph.yaml --version-label v0.1.0 --node https://subgraphs.alchemy.com/api/subgraphs/deploy --ipfs https://ipfs.satsuma.xyz --deploy-key NxUv3lhAaDS5x",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.79.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
